---
apiVersion: v1
kind: Service
metadata:
 name: nginx-service
spec:
 type: NodePort
 selector:
  app: nginx-app
 ports:
 - port: 8888 #Port Available inside cluster
   targetPort: 80 # Target Port on which container is listening
   nodePort: 32002 # Node Port
 
